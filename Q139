Show that enums store integers by printing assigned values.


#include <stdio.h>

enum TrafficLight {
    RED = 5,
    YELLOW = 10,
    GREEN = 15
};

int main() {
    printf("RED = %d\n", RED);
    printf("YELLOW = %d\n", YELLOW);
    printf("GREEN = %d\n", GREEN);

    return 0;
}
