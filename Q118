Q118. Write a program to take an input array of size n. The array should contain all the integers between 0 to n except for one. Print that missing number


#include <stdio.h>

int findMissingNumber(int arr[], int size, int n) {
    int total_sum = n * (n + 1) / 2;  // Sum of all numbers from 0 to n
    int arr_sum = 0;
    for (int i = 0; i < size; i++) {
        arr_sum += arr[i];
    }
    return total_sum - arr_sum;  // Missing number
}

int main() {
    int n;
    printf("Enter the value of n (total numbers from 0 to n): ");
    scanf("%d", &n);

    int size = n;  // Since one number is missing, input array size is n
    int arr[size];

    printf("Enter %d numbers between 0 to %d with one number missing:\n", size, n);
    for (int i = 0; i < size; i++) {
        scanf("%d", &arr[i]);
    }

    int missing = findMissingNumber(arr, size, n);
    printf("The missing number is: %d\n", missing);

    return 0;
}
