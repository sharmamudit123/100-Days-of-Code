Q119. Write a program to take an integer array as input. Only one element will be repeated. Print the repeated element. Try to find the result in one single iteration.


#include <stdio.h>

int findRepeatedElement(int arr[], int size) {
    int result = 0;
    for (int i = 0; i < size; i++) {
        result ^= arr[i]; // XOR with array elements
    }
    for (int i = 1; i <= size - 1; i++) {
        result ^= i; // XOR with numbers from 1 to size-1
    }
    return result;
}

int main() {
    int n;
    printf("Enter size of array (n): ");
    scanf("%d", &n);

    int arr[n];
    printf("Enter %d elements where exactly one element is repeated:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int repeated = findRepeatedElement(arr, n);
    printf("The repeated element is: %d\n", repeated);

    return 0;
}
